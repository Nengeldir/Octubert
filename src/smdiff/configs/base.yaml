# Common training defaults (mirrors legacy hparams defaults)
model: schmu_tx_vae
tracks: melody
bars: 64
NOTES: 1024  # derived bars*16 in legacy; kept for clarity
batch_size: 16
lr: 0.0005
warmup_iters: 10000
validation_set_size: 0.05
augment: true
ema: true
ema_beta: 0.995
amp: false
steps_per_log: 10
steps_per_eval: 20
steps_per_checkpoint: 50
steps_per_sample: 50
steps_per_update_ema: 10
sample_steps: 1024
log_base_dir: null
load_dir: null
load_step: 0
masking_strategy: null
sample_schedule: random
mask_schedule: random
sample_type: diffusion
loss_type: reweighted_elbo
attn_pdrop: 0.2
embd_pdrop: 0.2
resid_pdrop: 0.2
bert_n_emb: 512
bert_n_head: 8
bert_n_layers: 24
conv_layers: 1
conv_len: 4
sub_seq_len: 32
upper_bert_n_emb: 512
layers_per_level: 2
conv_width: 4
codebook_size: [90]
latent_shape: [1024,1]
dataset_path: data/POP909_melody.npy
port: 8097
